name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  check-and-test:
    name: Check & Test
    runs-on: ubuntu-latest
    if: |
      !startsWith(github.event.head_commit.message, 'chore: release')
    steps:
      - name: Setup Bun
        uses: kevinmarrec/workflows/setup-bun@df3f012b48666bfb006608cec9b9cb842b39eb50 # main

      - name: Check unused files, dependencies, and exports
        run: bun run check:unused

      - name: Lint
        run: bun run check:eslint

      - name: Type check
        run: bun run check:types

      - name: Unit tests
        run: bun run test:coverage
